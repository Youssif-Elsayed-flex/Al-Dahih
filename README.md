# 🎓 منصة الدحّيح التعليمية (Al-Dahih Platform)

منصة تعليمية متكاملة تهدف إلى توفير تجربة تعليمية متميزة للطلاب، وتسهيل إدارة العملية التعليمية للمدرسين والإداريين. المنصة مصممة بأحدث التقنيات لضمان السرعة، الأمان، وسهولة الاستخدام.

## ✨ المميزات الرئيسية

### 👨‍🎓 للطلاب:
*   **تصفح الدورات:** استعراض كافة الدورات المتاحة مع تصفية حسب الصف الدراسي والمادة.
*   **الحجز والدفع:** نظام حجز سهل ودفع إلكتروني (فودافون كاش) أو نقدي.
*   **لوحة تحكم:** متابعة الدورات المشترك بها، وسجل المدفوعات.
*   **الملف الشخصي:** إدارة البيانات الشخصية وتحديثها.

### 👨‍🏫 للمدرسين:
*   **إدارة الدورات:** إضافة وتعديل الدورات والمحتوى التعليمي.
*   **متابعة الطلاب:** كشف بأسماء الطلاب المشتركين.
*   **الحضور والغياب:** تسجيل حضور الطلاب (مخطط مستقبلي).

### 👮 للإدارة (Admin):
*   **التحكم الكامل (God Mode):** إدارة الموظفين، الطلاب، الدورات، والحجوزات.
*   **التقارير المالية:** لوحة إحصائيات شاملة للأرباح والحجوزات.
*   **إدارة الموظفين:** إضافة مدرسين ومحاسبين وتحديد صلاحياتهم.

### 💰 للمحاسبين:
*   **مراجعة المدفوعات:** تأكيد تحويلات فودافون كاش.
*   **تحصيل النقدي:** تسجيل المدفوعات النقدية للطلاب.

---

## 🛠️ التقنيات المستخدمة

### Frontend (الواجهة الأمامية):
*   **React + Vite:** لأداء فائق السرعة وتجربة مستخدم سلسة.
*   **Tailwind CSS:** لتصميم عصري ومتجاوب (Responsive) يدعم اللغة العربية (RTL).
*   **Framer Motion:** لإضافة تأثيرات حركية جذابة.
*   **TanStack Query:** لإدارة البيانات والتخزين المؤقت (Caching).
*   **Axios:** للتواصل مع السيرفر.

### Backend (الخلفية):
*   **Node.js + Express:** بيئة تشغيل قوية وسريعة.
*   **MongoDB + Mongoose:** قاعدة بيانات مرنة لتخزين البيانات.
*   **JWT + Cookies (httpOnly):** نظام حماية متطور لتأمين الجلسات.
*   **Cloudinary:** لتخزين وإدارة صور الدورات والمستخدمين.

---

## 🚀 طريقة التشغيل (Quick Start)

1.  **تثبيت المكاتب:**
    ```bash
    # install server dependencies
    cd server
    npm install

    # install client dependencies
    cd ../client
    npm install
    ```

2.  **إعداد البيئة (.env):**
    *   قم بإنشاء ملف `.env` في مجلد `server` وضع فيه رابط قاعدة البيانات `MONGO_URI`.

3.  **تشغيل المشروع:**
    ```bash
    # Terminal 1 (Backend)
    cd server
    npm run dev

    # Terminal 2 (Frontend)
    cd client
    npm run dev
    ```

---

## 🔐 حساب المسؤول الافتراضي (Default Admin)

عند تشغيل السيرفر لأول مرة، يتم إنشاء حساب مدير تلقائياً:
*   **البريد:** `admin@eldahih.com`
*   **كلمة المرور:** `admin123`

---

## 📞 الدعم

لأي استفسارات تقنية أو مشاكل، يرجى التواصل مع فريق التطوير.
# -
# -
